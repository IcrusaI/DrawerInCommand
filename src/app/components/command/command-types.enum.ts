import { ShapeTypes } from '../shape/shape-types.enum';

export enum CommandTypes {
  RECTANGLE = ShapeTypes.RECTANGLE,
  TRIANGLE = ShapeTypes.TRIANGLE,
  CIRCLE = ShapeTypes.CIRCLE,
  ELLIPSE = ShapeTypes.ELLIPSE,
  LINE = ShapeTypes.LINE,
}
